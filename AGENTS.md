# AGENTS.md

This repository is a static website for GitHub Pages.

## Guidelines

- Keep the site deployable as plain static assets.
- Prefer simple HTML, CSS, and JavaScript unless a build step is clearly needed.
- Do not commit generated output, dependencies, logs, or local environment files.
